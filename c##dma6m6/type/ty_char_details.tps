create or replace type ty_char_details is object(
       char_id number,
       weapon_id number,
       weapon_name varchar2(250 char),
       char_name varchar2(250 char),
       char_lvl number
);
/
